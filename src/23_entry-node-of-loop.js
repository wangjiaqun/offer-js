/**
 * @description 链表中环的入口结点
 * 给一个链表，若其中包含环，请找出该链表的环的入口结点，否则，输出null。
 * @author wangjiaqun
 * @param
 * @return
 */
function ListNode(x) {
    this.val = x;
    this.next = null;
}
function entryNodeOfLoop(pHead) {
    // write code here
}